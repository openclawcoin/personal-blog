{
  "name": "personal-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "prebuild": "npm run check:posts",
    "preview": "astro preview",
    "check": "astro check",
    "check:posts": "node scripts/validate-blog-frontmatter.mjs",
    "new:daily": "powershell -ExecutionPolicy Bypass -File scripts/new-daily-post.ps1"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
